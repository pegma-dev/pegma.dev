# Vendored Health package

`pegma-health-0.1.0.tgz` is the exact npm-pack artifact from
`pegma-dev/health` commit `35a878b` (`@pegma/health` 0.1.0).

Generated with:

```text
npm ci
npm pack --workspace @pegma/health --pack-destination <pegma.dev>/worker-vendor
```

Remove this bridge when `@pegma/health` has a public npm release.
